# Αντίγραψέ το σε .env δίπλα στο compose.yml και συμπλήρωσε.
# ΠΡΟΣΟΧΗ: το .env δεν πρέπει να μπει σε git.

# Υποχρεωτικό - τυχαία μυστική τιμή (π.χ. output από: openssl rand -hex 32)
FLASK_SECRET=change-me